{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-shared-library",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-shared",
                "-o",
                "${workspaceFolder}/sortingLib.dll",
                "${workspaceFolder}/impSort.c"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
